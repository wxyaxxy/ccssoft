2020-10-19版本：
1. 将照片存储在U盘或外部SD卡中
2. 程序每秒线程中加入SD卡情况检查
2.1 如果未检测到SD卡，则在心跳包中提示错误
2.2 检查SD卡容量使用情况，将已使用容量百分比放在心跳包中上传到服务器
3. 增加读取照片存储情况的指令，read_SD_info，可以获得每个任务号下照片的容量大小
4. 增加删除指定任务号目录的指令，delete_photo，可以远程删除指定任务号下所有的照片，释放空间

2020-10-20版本：
1. 增加app修改图传地址功能
2. 增加app修改数传地址（三元组）功能
3. 增加软重启指令，修改三元组后用新的三元组连接到服务器
4. 增加“上锁测试”和“解锁测试”指令

2020-10-21版本：
1. 优化下发命令应答机制
2. 一键起飞命令，要先发送解锁命令，如果解锁失败，则不发送起飞指令
3. 如果解锁成功，才下发起飞指令
4. 一键起飞失败，通过Details字段告知失败原因

2020-10-22版本：
1. 核对航线
2. 图传验证不占用传心跳线程
3. 程序重启通过巡航状态开启拍照、传照和保存TRACE线程
4. 记住上次的任务号
5. 重启优化，根据记住的任务号去查询拍了多少张照片


2020-10-23版本：
1. 下发完航线后自动开始核对，回复核对结果
2. 照片exif信息中方向写入,Orientation保持为0，让照片横着
3. trace_save线程启动时加头，最后传输的时候加尾改为发送的时候在加头和尾巴，巡航时按行存心跳
4. trace文件如果存在则append,重启优化
5. 自检量读取和传输，卫星数，电压什么的，初始值为null
6. 长时间断网os.execl()自己


2020-10-26版本：
1. trace_save线程启动时加头，最后传输的时候加尾改为发送的时候在加头和尾巴，巡航时按行存心跳
2. trace文件如果存在则append,重启优化
3. 测绘版本软件给程锡东

2020-10-27版本：
1. 模拟器更新核对航线
2. 任务号错误检测，根据图传给的任务号，修改当前执行任务号
3. 核对航线仅核对4个点，x,y,z,command
3. 自检量读取和传输，卫星数，电压什么的，初始值为null


2020-10-28版本：
1. 下发航线清除航线增加延时
2. iot_reply_command增加参数detail，为中文，app直接显示
3. 核对航线，经纬度误差为1，也算核对成功

2020-10-29版本：
1. 心跳中清除掉UAV_Type
2. 阿里云IOT初始化修改发送缓冲区为1，接收缓冲区保持为0（no limit）,qos修改为0，解决网络不好导致心跳乱序的问题
3. 断网复位优化，加快恢复网络速度

2020-10-30版本：
1. SD卡检测逻辑，/media/pi/SDCARD没有umount会一直存在，df -h返回为空
2. 阶段性功能测试
3. 3D打印C版本设备底盒

2020-11-02版本：
2. 模拟地面站的软件
3. 软件补丁升级方式
5. 守护进程 网络连接设置为守护进程
6. 应用程序设置为守护进程
7. xc阶段性软件升级，为了异地起降测试

2020年11月06日：
1. 继电器控制VBUS断开和闭合，BCM26 低电平导通，高电平断开
2. 测绘方案暂停，可以快速拍照但是不能快速传输

2020-11-10：
1. 心跳发送加缓冲，不掉数据 or 客服推荐配置 断网测试
3. 主副卫星数获取，等待智能鸟更新固件

2020-11-12：
1. SD卡开机可以自动挂载，中途拔掉再插入就无法自动挂载
3. 各种消息回复错误信息

2020-11-16：
1. U盘自动挂载BUG修复，上电自动挂载，热插拔自动挂载
2. 客服推荐配置 断网测试 clean_session=false qos=1不行，会混乱
3. 心跳发送加缓冲，不掉数据
3.1 断网检测和心跳缓存分开线程，每秒心跳都会缓存到list
3.2 增加发送心跳线程，发送list不加延时，直到list为空
3.3 resetNet后，自动disconnect特别慢，手动disconnect然后connect,加快断网恢复

2020-11-17：
1. 各种消息回复错误信息，SD卡不存在则不拍照，报错“SD未插入”
2. V3.0调试连接XC飞控，调试最新功能
4. XC新版本，只有飞控，没有测试环境

2020-11-17傲势版本：
1. 复制最新P330的程序，修改其中跟傲势不同的地方
1.1 解析航线
1.2 读取飞控不一样
1.3 核对航线未测试

2020-11-18傲势版本：
1. 可以正确下发航线，也可以正确核对航线
2. 将电池电压加入到心跳包中,电量大于100等于100，小于0等于0，当前无电压
3. 跟盒子安装花生壳，安装不上，测试下ttl调试口
4. usb转串口偶尔被识别为ttyUSB4，偶尔被识别为ttyUSB0,拨号用ttyUSB3也可以拨号成功。。

5.上传到github，方便更新固件和initial.init和其他
2. MISSION包，飞控模式，拍照模式，视频模式

飞控模式
照片模式
视频模式
等距拍照
等时拍照